#!/usr/bin/env bash
set -ue

# Switch to zsh as default sheel
grep {{ .chezmoi.username }} /etc/passwd | grep /usr/bin/zsh > /dev/null
if [ $? -ne 0 ]; then
    echo "Changing user shell to zsh..."
    chsh -s /usr/bin/zsh
fi
